#include <stdio.h>


int main()

{
           int i;
            int j;
    int sieve[50];

        sieve = ['true'] * (50+1)
        sieve [0] = sieve [1] = false
        i=2
      {
           while (i*i<=50):
                if (sieve[i]):
                    k = i*i
      }
                   {
                     while (k<=50):
                        sieve [k]= false
                        k+= i;
        i+=1;

                   }
}
  return sieve;
